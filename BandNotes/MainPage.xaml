<Page
    x:Class="BandNotes.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BandNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="sendButton" Icon="Send" Label="send" Click="AppBarButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <Pivot Title="BAND NOTES">
            <PivotItem Header="send">
                <TextBox x:Name="txtBody" TextWrapping="Wrap" VerticalAlignment="Stretch" AcceptsReturn="True" PlaceholderText="Enter a message to send"/>
            </PivotItem>
            <PivotItem Header="options">
                <Grid/>
            </PivotItem>
        </Pivot>

    </Grid>
</Page>
